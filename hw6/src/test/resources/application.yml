spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:library
    data: test_data.sql
    initialization-mode: always
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true